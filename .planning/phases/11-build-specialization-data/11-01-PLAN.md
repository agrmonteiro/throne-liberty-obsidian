---
phase: 11-build-specialization-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/store/useBuilds.ts
autonomous: true
requirements:
  - ROT-01

must_haves:
  truths:
    - "O tipo Build compila sem erros TypeScript com os três novos campos opcionais"
    - "Builds antigas carregadas do disco não quebram — campos ausentes chegam como undefined"
    - "parseNewScraperFormat() extrai specialization, weaponMain e weaponOff do output do scraper"
    - "Objetos malformados em specialization são filtrados pelo type guard — apenas {id: string, lvl: number} são aceitos"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "Interface Build com specialization?, weaponMain?, weaponOff?"
      contains: "specialization?: Array<{ id: string; lvl: number }>"
    - path: "src/store/useBuilds.ts"
      provides: "Extração dos novos campos em parseNewScraperFormat()"
      contains: "weaponMain: typeof raw.weaponMain"
  key_links:
    - from: "src/store/useBuilds.ts (parseNewScraperFormat)"
      to: "src/engine/types.ts (Build interface)"
      via: "retorno tipado como Build"
      pattern: "specialization.*Array.*id.*string.*lvl.*number"
---

<objective>
Estender o modelo de dados TypeScript com três campos opcionais de especialização e atualizar o parser do scraper para extraí-los.

Purpose: Habilitar a Phase 12 (Rotation Builder) a ler build.specialization, build.weaponMain e build.weaponOff ao importar uma build para uma rotação.

Output: types.ts com interface Build estendida; useBuilds.ts com extração em parseNewScraperFormat(). Zero quebras em builds existentes.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-build-specialization-data/11-CONTEXT.md
@.planning/phases/11-build-specialization-data/11-RESEARCH.md
@.planning/phases/11-build-specialization-data/11-PATTERNS.md

<interfaces>
<!-- Interfaces extraídas de src/engine/types.ts (linhas 60-73) — estado atual -->

```typescript
// src/engine/types.ts — interface Build ATUAL (antes desta fase)
export interface Build {
  id:             string
  name:           string
  weaponCombo:    string
  stats:          BuildStats
  notes:          string
  importedAt:     string  // ISO date
  editedAt?:      string
  sourceUrl?:     string
  screenshotFile?: string
  rawAttributes?: Record<string, { total: number; display: string }>
  rawStats?:      Record<string, string>
}

// NOTA D-06: weaponMainType/weaponOffType existem em RotationCharacter (linhas 83-86)
// com formato capitalizado ("Staff", "Wand & Tome"). Os novos campos weaponMain/weaponOff
// são DISTINTOS — espelham o formato Questlog lowercase ("staff", "wand").
```

<!-- Ponto de inserção em parseNewScraperFormat() (src/store/useBuilds.ts linhas 332-343) -->

```typescript
// ATUAL — return de parseNewScraperFormat() (linha 332)
return {
  id:            newId(),
  name,
  weaponCombo:   '',
  stats,
  notes:         '',
  importedAt:    new Date().toISOString(),
  sourceUrl,
  rawStats:      rawStatsIn,
  rawAttributes,
}

// Padrão de type guard inline já usado no arquivo (linha 330):
const sourceUrl = typeof meta.source_url === 'string' ? meta.source_url : undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender interface Build em types.ts (D-04, D-05, D-06)</name>
  <read_first>
    - src/engine/types.ts (arquivo completo — verificar campos existentes antes de adicionar)
  </read_first>
  <files>src/engine/types.ts</files>
  <behavior>
    - O tipo `Build` aceita `specialization?: Array<{ id: string; lvl: number }>` sem erro de compilação
    - O tipo `Build` aceita `weaponMain?: string` e `weaponOff?: string` sem erro de compilação
    - Um objeto Build sem os novos campos (builds antigas) satisfaz a interface sem erro
    - Os campos novos são distintos de `weaponCombo` (já existente) e de `RotationCharacter.weaponMainType`/`weaponOffType`
  </behavior>
  <action>
Ler src/engine/types.ts e localizar o final da interface `Build` (após `rawStats?: Record<string, string>`).

Adicionar exatamente estes três campos após `rawStats?:`, seguindo o mesmo estilo de indentação do arquivo (2 espaços, `?:` alinhado com os outros campos opcionais):

```typescript
  // Phase 11 — dados de especialização do Questlog Build Editor (per D-04, D-06)
  specialization?: Array<{ id: string; lvl: number }>
  weaponMain?:     string   // ex: "staff" (lowercase, formato Questlog) — distinto de RotationCharacter.weaponMainType
  weaponOff?:      string   // ex: "wand" (lowercase, formato Questlog) — distinto de RotationCharacter.weaponOffType
```

NÃO modificar `RotationCharacter`, `BuildStats`, `DEFAULT_STATS` nem nenhum outro tipo.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/OneDrive/Documentos/python/throne_and_liberty_node" && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/types.ts contém a linha `specialization?: Array<{ id: string; lvl: number }>`
    - src/engine/types.ts contém a linha `weaponMain?:     string`
    - src/engine/types.ts contém a linha `weaponOff?:      string`
    - `npx tsc --noEmit` encerra com código 0 (sem erros de tipo)
    - A interface `Build` ainda contém todos os campos anteriores intactos (id, name, weaponCombo, stats, notes, importedAt, editedAt?, sourceUrl?, screenshotFile?, rawAttributes?, rawStats?)
  </acceptance_criteria>
  <done>Interface Build estendida com 3 campos opcionais; TypeScript compila sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extrair specialization/weaponMain/weaponOff em parseNewScraperFormat() (D-02, D-05)</name>
  <read_first>
    - src/store/useBuilds.ts (inteiro — verificar o return atual de parseNewScraperFormat() e o padrão de type guard inline)
    - src/engine/types.ts (confirmar que Task 1 foi concluída antes de editar)
  </read_first>
  <files>src/store/useBuilds.ts</files>
  <behavior>
    - Quando raw.specialization é `[{id: "Wand_Normal_Tactic_11", lvl: 10}]`, o retorno tem `specialization: [{id: "Wand_Normal_Tactic_11", lvl: 10}]`
    - Quando raw.specialization é `null`, `undefined`, ou array vazio, o retorno tem `specialization: undefined`
    - Quando raw.specialization contém objeto malformado `{id: 123, lvl: "x"}`, ele é filtrado — apenas itens válidos são incluídos
    - Quando raw.weaponMain é `"staff"`, o retorno tem `weaponMain: "staff"`
    - Quando raw.weaponMain é `undefined` ou não-string, o retorno tem `weaponMain: undefined`
    - `parseOldFormat()` NÃO é alterado — builds no formato antigo não recebem specialization
    - `validateScraperOutput()` NÃO é alterado
  </behavior>
  <action>
Ler src/store/useBuilds.ts e localizar a função `parseNewScraperFormat()` (linhas ~257-343).

Antes do `return { ... }` final da função, adicionar estas variáveis locais (após a definição de `rawAttributes`, seguindo o estilo de variáveis locais do arquivo):

```typescript
  const rawSpec = raw.specialization
  const specialization: Array<{ id: string; lvl: number }> | undefined = (() => {
    if (!Array.isArray(rawSpec) || rawSpec.length === 0) return undefined
    const filtered = (rawSpec as unknown[]).reduce<Array<{ id: string; lvl: number }>>((acc, item) => {
      if (
        item !== null && typeof item === 'object' &&
        typeof (item as any).id === 'string' &&
        typeof (item as any).lvl === 'number'
      ) {
        acc.push({ id: (item as any).id, lvl: (item as any).lvl })
      }
      return acc
    }, [])
    return filtered.length ? filtered : undefined
  })()
```

Depois, no objeto `return { ... }`, adicionar os três campos APÓS `rawAttributes,`:

```typescript
    specialization,
    weaponMain: typeof raw.weaponMain === 'string' ? raw.weaponMain : undefined,
    weaponOff:  typeof raw.weaponOff  === 'string' ? raw.weaponOff  : undefined,
```

NÃO modificar `parseOldFormat()`, `validateScraperOutput()`, `loadFromDisk()` nem qualquer outra função.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/OneDrive/Documentos/python/throne_and_liberty_node" && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/store/useBuilds.ts contém `typeof raw.weaponMain === 'string' ? raw.weaponMain : undefined`
    - src/store/useBuilds.ts contém `typeof raw.weaponOff  === 'string' ? raw.weaponOff  : undefined`
    - src/store/useBuilds.ts contém `Array.isArray(rawSpec)` (parte do type guard de specialization)
    - `npx tsc --noEmit` encerra com código 0
    - `parseOldFormat` NÃO contém as palavras "specialization", "weaponMain" ou "weaponOff" (confirmar via grep)
    - `validateScraperOutput` permanece inalterada (não contém "specialization")
  </acceptance_criteria>
  <done>parseNewScraperFormat() extrai os três campos com type guards; TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper stdout → parseNewScraperFormat | JSON bruto do processo Python entra como Record<string, unknown> — não confiável |
| raw.specialization → Build.specialization | Array não tipado vindo do scraper deve ser validado item a item |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | parseNewScraperFormat — raw.specialization | mitigate | Type guard inline rejeita qualquer item onde id não seja string ou lvl não seja number — itens malformados são descartados silenciosamente |
| T-11-02 | Tampering | parseNewScraperFormat — raw.weaponMain/raw.weaponOff | mitigate | `typeof === 'string'` antes de aceitar — qualquer outro tipo resulta em undefined |
| T-11-03 | Tampering | Build.specialization[].id como path de arquivo | accept | specialization.id nunca é usado como path de arquivo — é ID de nó de maestria (ex: "Staff_Normal_Attack_01") — risco inexistente nesta fase |
| T-11-04 | Information Disclosure | specialization persistida em builds.json | accept | builds.json é armazenado em AppData local do usuário — sem transmissão de rede, sem PII nesta fase |
</threat_model>

<verification>
Após completar ambas as tasks:

```bash
cd "C:/Users/admin/OneDrive/Documentos/python/throne_and_liberty_node"

# 1. TypeScript limpo
npx tsc --noEmit

# 2. Build completo sem erro
npm run build

# 3. Verificações estruturais
grep -n "specialization" src/engine/types.ts
grep -n "weaponMain" src/engine/types.ts
grep -n "weaponOff" src/engine/types.ts
grep -n "specialization" src/store/useBuilds.ts
grep -n "parseOldFormat" src/store/useBuilds.ts | head -5
# Confirmar que parseOldFormat NÃO tem "specialization"
grep -n "specialization" src/store/useBuilds.ts | grep -v "parseNewScraperFormat\|rawSpec\|filtered\|acc\|item\|Array"
```
</verification>

<success_criteria>
- `npx tsc --noEmit` encerra com código 0
- `npm run build` encerra com código 0
- `src/engine/types.ts` contém `specialization?: Array<{ id: string; lvl: number }>`
- `src/store/useBuilds.ts` extrai specialization/weaponMain/weaponOff apenas em `parseNewScraperFormat`
- `parseOldFormat` não toca nos novos campos
- `validateScraperOutput` não toca nos novos campos
- Builds antigas (sem os campos) carregam sem erro (campos chegam como undefined via spread do loadFromDisk)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/11-build-specialization-data/11-01-SUMMARY.md` com:
- Campos adicionados ao tipo Build
- Localização exata da extração em parseNewScraperFormat (número de linha)
- Resultado de `npx tsc --noEmit`
- Resultado de `npm run build`
</output>
